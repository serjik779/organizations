{% extends 'AppBundle::base.html.twig' %}
{% block body %}
    {% form_theme form 'bootstrap_3_layout.html.twig' %}
    <h1>Создание работника</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-default" type="submit" value="Создать" />
        <a class="btn btn-default" href="{{ path('users_index') }}">Вернуться</a>
    {{ form_end(form) }}

{% endblock %}
