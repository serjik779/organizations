{% extends 'AppBundle::base.html.twig' %}
{% block body %}
    {% form_theme form 'bootstrap_3_layout.html.twig' %}
    <h1>Создание организации</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Создать" />
    {{ form_end(form) }}

    <a class="btn btn-default" href="{{ path('organizations_index') }}">Вернуться</a>
{% endblock %}
