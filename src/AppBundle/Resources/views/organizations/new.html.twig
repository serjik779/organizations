{% extends 'base.html.twig' %}
{% block body %}
    {% form_theme form 'bootstrap_3_layout.html.twig' %}
    <h1>{{ 'create.organization'|trans }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-default" type="submit" value="{{ 'create'|trans }}" />
    {{ form_end(form) }}

    <a class="btn btn-default" href="{{ path('organizations_index') }}">{{ 'back.to.list'|trans }}</a>
{% endblock %}
